import { SafeAreaView } from "react-native"

import Login from "../modules/login"

export default function App () {
    return (
        <SafeAreaView>
            <Login />
        </SafeAreaView>
    )
}
